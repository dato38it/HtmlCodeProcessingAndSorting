Напсать программу, которая
- убрает лшне дефсы в начале строк
- ставт точку в конце строк, есл его нету в раделе Desision
- Найт повторяющеся слова в раделе Task удалть то вместе с Desision
- по следущющем шаблону сортрует текст:
	Task:
	-
	Translation of the Task:
	-
	Decision:
	-
	Translation of the Decision:




Task:
Куб данных
Decision:
массив данных, определенный в измерениях или слоях.
Task:
Объект Model в Excel, реализованный надстройкой PowerPivot
Decision:
- обеспечивает основу для загрузки и объединения исходных данных из нескольких источников данных для анализа данных на рабочем столе, включая реляционные базы данных, многомерные источники, облачные службы, веб-каналы данных, файлы Excel, текстовые файлы и данные из Интернета. Excel интегрирует дополнительные источники данных и позволяет объединять данные из нескольких источников данных.
- предназначена для программного создания связей между таблицами модели, в результате чего создаются объединенные таблицы, объединение сводных таблиц и т. д.
Task:
Метаданные объекта 
Decision:
в объектном хранилище нужны, чтобы описывать свойства объекта
Task:
Inner join
Decision:
Возвращаются только те строки, где ключевые значения совпадают в обеих таблицах.
Task:
Self Join
Decision:
это операция объединения таблицы с самой собой. Это означает, что при SELF JOIN таблица используется дважды в одном запросе. Чтобы не путаться, каждое вхождение таблицы в команде использует псевдоним.
используется в случаях, когда нужно проанализировать зависимости внутри одной таблицы. Самый частый пример использования SELF JOIN — обработка деревьев или графов, которые хранятся в одной таблице.
Task:
Cross Join
Decision:
позволяет получить декартово произведение (результат соединения строки из первой таблицы с каждой строкой из второй  таблицы) нескольких таблиц. особенно полезен, когда между таблицами нет определенной связи, и вам нужно создать полную комбинацию записей из каждой таблицы.
Task:
Left Outher Join 
Decision:
используется для объединения двух таблиц, но с сохранением всех записей из левой таблицы, в том числе и тех, для которых нет соответствующих записей в правой таблице.
Он возвращает все строки из левой таблицы и совпадающие строки из правой таблицы. Если совпадений нет, то для столбцов правой таблицы используются значения NULL.
Task:
Right Outher Join 
Decision:
используется для объединения двух таблиц, но для сохранения всех записей из правой таблицы, в том числе и тех, для которых нет соответствующих записей в левой таблице.
Он возвращает все строки из правой таблицы и совпадающие строки из левой таблицы. Если совпадений нет, то для столбцов левой таблицы используются значения NULL.
Task:
Full Outher Join 
Decision:
используется для объединения двух таблиц с сохранением всех записей, включая те, для которых нет соответствующих записей в другой таблице.
Он возвращает все строки, если есть совпадение либо в левой, либо в правой таблице. Если совпадения нет, то в столбцах таблицы, в которой нет совпадения, используются значения NULL.
Task:
Реляционная бд
Decision:
представляют собой базы данных, которые используются для хранения и предоставления доступа к взаимосвязанным элементам информации.
В рамках SQL используются также встроенные языки реляционных баз данных: DDL для таблиц (применяют для описания данных) и DML для работы с данными.
Task:
Нереляционная бд
Decision:
В них хранящаяся информация напрямую зависит от особенностей самой базы данных. К примеру, в случае с документоориентированной БД информация может быть представлена в виде иерархии и описывать различные объекты с разными параметрами.
Task:
репликации БД
Decision:
набор технологий, позволяющий копировать, распространять и синхронизировать определенные типы объектов базы данных и связанных с ними данных и зависимостей между ними из одной базы данных в одну или несколько других баз данных в одном и том же экземпляре или в разных экземплярах SQL Server
Task:
Транзакция БД
Decision:
набор операций по работе с базой данных (БД), объединенных в одну атомарную пачку.
Транзакция - архив для запросов к базе. Он защищает ваши данные благодаря принципу "всё, или ничего".
Task:
MongoDB
Decision:
документо-ориентированная Модель данных
Task:
ElasticSearch
Decision:
Система нереляционных баз, распространяемая с открытым исходным кодом. Представляет собой интерфейс протокола передачи гипертекста и бесплатных документов.
Task:
ClickHouse
Decision:
для онлайн-обработки аналитических запросов
Task:
Метаданные объекта 
Decision:
в объектном хранилище нужны, чтобы описывать свойства объекта
Task:
Amazon DynamoDB
Decision:
ПО, принадлежащее Amazon, предназначенное для использования в небольших приложениях. Позволяет обрабатывать огромное количество инструкций в кратчайшие сроки.
Task:
Cassandra
Decision:
Система БД с открытым кодом, разработанная в Facebook. Дает возможность эффективно управлять огромными массивами данных. Используется сотнями компаний по всему миру.
Task:
HBase
Decision:
Система на Java с открытым кодом и практически бесконечными возможностями масштабирования.
Task:
репликация моментального снимка
Decision:
позволяет распространять данные на определенный момент времени. Снимок пересылается и более не обновляется процессом репликации, пока не будет сделан новый снимок, который будет отправлен подписчику. Репликация моментального снимка обычно используется для формирования схемы статей и связанных с ней данных у подписчика для репликации транзакций и репликации слиянием.
Task:
репликация транзакций
Decision:
может обеспечивать потоковое изменение данных с низким уровнем задержек, выполняемое издателем для одного или более подписчиков.
Task:
репликация слиянием
Decision:
позволяет подписчику напрямую модифицировать данные подписки (статьи) и затем синхронизировать модифицированные строки с издателем. Как указывалось ранее, подписчики могут быть некоторое время отключены от коммуникаций, периодически подключаться и синхронизировать данные по запросу. В этот момент их изменения синхронизируются с издателем и наоборот. Это может привести к конфликту данных, когда издатель и один или более подписчиков пытаются модифицировать одни и те же данные.
Task:
PowerPivot (PPM)
Decision:
Создание и удаление модели
Task:
Метод Model.AddConnection
Decision:
Добавляет новое подключение к книге к модели с теми же свойствами, что и у указанного в качестве аргумента. 
Task:
Метод Model.CreateModelWorkbookConnection
Decision:
Возвращает объект WorkbookConnection типа ModelConnection
Task:
Метод Model.Initialize
Decision:
Инициализирует PPM.
Task:
Метод Model.Refresh
Decision:
Обновляет все источники данных, связанные с моделью, полностью обрабатывает модель и обновляет все функции данных Excel, связанные с объектом Model .
Task:
Объект ModelChanges
Decision:
содержит сведения о том, какие изменения были внесены в модель данных при возникновении события Workbook.ModelChange после операции модели.
Task:
Коллекция ModelColumnChanges
Decision:
представляют столбцы, для которых тип данных был изменен в PPM. 
Task:
Метод ModelColumnChanges.Item
Decision:
Возвращает один объект из коллекции ModelColumnChanges
Task:
Объект ModelColumnChange
Decision:
Объект , представляющий столбец в таблице в PPM, для которого был изменен тип данных.
Task:
Коллекция ModelColumnNames
Decision:
Коллекция, представляющая столбцы таблиц в PPM.
Task:
Метод ModelColumnNames.Item
Decision:
Возвращает один объект из коллекции ModelColumnNames
Task:
Объект ModelColumnName
Decision:
Объект , представляющий имя столбца в PPM.
Task:
Объект ModelConnection
Decision:
содержат сведения о новом типе подключения модели, представленном в Excel для взаимодействия со встроенным PPM.
Task:
Коллекция ModelMeasureNames
Decision:
содержит коллекцию объектов ModelMeasureName в PPM.
Task:
Метод ModelMeasureNames.Item
Decision:
Возвращает один объект из коллекции ModelMeasureNames.
